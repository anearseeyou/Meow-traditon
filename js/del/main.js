/**
 * Javascript  Document
 */

// Tab路由切换
/*

$('.nav').on('click', 'span', function () {
    $('.nav span').removeClass('current');
    $(this).addClass('current');
});

// 音乐\故事切换
$('.changeClick').on('click', function () {
    $('.changeClick').removeClass('current');
    $(this).addClass('current');
});

// 分享内容显示
$('.share-btn').on('click', function () {
    $('.float-layer').show();
    $('.share-layer').height(27.5 + "rem");
});

// 分享内容隐藏
$('.share-close-bar').on('click', function () {
    $('.float-layer').hide();
    $('.share-layer').height(0);
});

// 发布评论
$('.icon-write-bar').on('click', function () {
    $('.float-layer').show();
    $('.write-rat').show();
});
$('.write-close').on('click', function () {
    $('.float-layer').hide();
    $('.write-rat').hide();
});

// 我的评论切换
changeHideOrShow($('.tab-list'), 'cur-underline', $('.showOrHide'));

function changeHideOrShow(clickObject, addedClass, isShowObject) {
    clickObject.on('click', function () {
        var key = $(this).index();
        $(this).addClass(addedClass).siblings().removeClass(addedClass);
        $(isShowObject).hide();
        $(isShowObject).eq(key).show();
    });
}*/
